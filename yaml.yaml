dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^3.3.3
  cloud_firestore: ^3.2.2
  http: ^0.13.3
  cached_network_image: ^3.1.0
  webview_flutter: ^2.0.13
  provider: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
